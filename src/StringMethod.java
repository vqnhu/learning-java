public class StringMethod {
    public static void main(String[] args) {
        // String = a reference data type that can store one or more characters
        //             reference data types have access to useful methods

        String name = "Nhu";

        // boolean result = name.equalsIgnoreCase("nhu"); --> result: true
        // equalIgnoreCase = make the bollean not case sensitive

        // int result = name.length(); --> result: 3

        // char result = name.charAt(0); --> result: N
        // charAt = the character place in '( )';

        // int result = name.indexOf("h"); --> result: 1
        // indexOf = opposite of charAt, finds which place the character is

        // boolean result = name.isBlank(); result: false

        // String result = name.toUpperCase(); result: NHU

        // String result = name.toLowerCase(); result: --> nhu

        // String result = name.trim() --> remove all the empty spaces

        // String result = name.replace("h", "o"); result: Nou

        // System.out.println(result);

    }
}
